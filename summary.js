/*
fundamentals

1.1: varialbe(let,const) three types of variable
1.2: condition(<,>, ===, !==, <=,>=) &&, ||, if-else
1.3: array: declare, length, index, push, pop, indexOf, includes
1.4: for loop, while
1.5: function return parameter
1.6: object property, including array, object

----------------

Es6:

2.1 templet string${}
2.2 spred operator(...)copy an array and then  add a new rlement to an array
2.3 user filter to remove an element from an array
3. arrow function
3.1 no parameter()
3.2 single parameter
3.3 multiple parameter()
3.4 multi line

4. destructuring: object destructuring, array destructuring

5. object shorthand
6. function parameter default value
7. optional chaining(?.)
---------------

browser API
1. local storage session storage
2. location API
3. History API
4. fetch

otheres:
array: map,forEach, filter, find
2. ternery operator
3. logical && and logical or||
4. JSON(stringyfy, parse)
5. +,


*/

const x = 50;
const y = 100;
const obj = {
    x: x,
    y: y
};

// shortcut
const obj1 = {x,y}

const name = 'solimullah';
const area = 'dhaka';

const nobab = {
    name: name,
    area: area,
}

//shortcut
const nobab1 = {name,area}
